#include <iostream>
#include <string.h>
#include <windows.h>

#pragma once

class Printer {
 public:
  Printer(void);
  ~Printer(void);

  void print(void);

  char buffer[100];
 private:
  // char buffer[100]; ??????????????????????????? Что лучше? private и get_buffer или public ?
  //struct Pixel { int row, int column} buffer[100]; ????????
};