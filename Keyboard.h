#include <conio.h>

#pragma once

class Keyboard {
 public:
  Keyboard();
  ~Keyboard();

  enum class key { left, right, up, down, any, esc, none };
  key code;  //???????????????????? Why can't go after?

  void input(void);  ///////////???????? Keyboard::key ?

 private:
};
